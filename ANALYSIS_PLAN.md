# 창원시 안전 인프라 분석 설계안

## 1. 연구 질문

창원시를 100m 격자로 나누고 CCTV, 보안등, 공공 Wi-Fi의 접근성과 생활안전지도의 범죄위험도를 결합하여 다음을 확인한다.

1. 안전 인프라 접근성이 높은 격자는 유사한 지역 특성을 가진 격자보다 범죄위험도가 낮은가?
2. CCTV·보안등·공공 Wi-Fi가 모두 갖춰진 격자는 각 시설의 개별 효과를 합한 것보다 추가적인 결합효과가 있는가?
3. 현재 범죄위험은 높지만 안전 인프라가 부족하여 우선 개선이 필요한 구역은 어디인가?

관측자료만 사용할 때 결과는 **인과효과**가 아니라 **공간적 연관성**으로 표현한다. 시설이 원래 위험한 곳에 우선 설치됐을 가능성이 있기 때문이다.

## 2. 분석 단위

- 주 분석: 100m × 100m 격자
- 민감도 분석: 50m 및 200m 격자에서 결과 순위가 유지되는지 확인
- 분석 대상: 창원시 행정경계 안의 생활권 격자
- 제외 또는 별도 표시: 바다, 산림, 산업시설 내부처럼 상주·유동인구 노출이 거의 없는 격자
- 최종 Top 10: 인접한 고위험 격자는 하나의 위험구역으로 합쳐 같은 장소가 여러 순위를 차지하지 않게 처리

## 3. 시설 변수

각 격자 중심에서 시설별 최근접 거리와 반경 내 개수를 계산한다.

| 시설 | 기본 영향 반경 | 초기 가중치 | 변수 구성 |
|---|---:|---:|---|
| CCTV | 100m | 0.45 | 최근접 거리, 반경 내 카메라 수 |
| 보안등 | 50m | 0.45 | 최근접 거리, 반경 내 보안등 수 |
| 공공 Wi-Fi | 100m | 0.10 | 최근접 거리, 반경 내 고유 설치지점 수 |

초기 가중치는 공모전 시제품을 위한 정책 가중치다. 공공 Wi-Fi는 직접적인 감시·조명 시설이 아니므로 낮게 둔다. 최종 가중치는 범죄 결과변수를 확보한 뒤 교차검증으로 다시 추정한다.

시설별 점수는 다음처럼 0~100으로 만든다.

```text
거리점수_f = max(0, 1 - 최근접거리_f / 영향반경_f)
밀도점수_f = 1 - exp(-반경 내 시설수_f / 기준개수_f)
시설점수_f = 100 × (0.6 × 거리점수_f + 0.4 × 밀도점수_f)
보호점수 P = 0.45 × CCTV점수 + 0.45 × 보안등점수 + 0.10 × Wi-Fi점수
```

기준개수는 전체 격자의 중앙값 또는 상위 25% 값을 사용하여 한 장소의 과도한 시설 중복이 점수를 지배하지 않게 한다. 같은 좌표의 Wi-Fi AP는 고유 설치지점 하나로 묶고 CCTV는 위치 수와 카메라 대수를 따로 보존한다.

## 4. 범죄 결과변수

가장 좋은 결과변수는 격자별·기간별 범죄 발생 건수다. 이 경우 인구 또는 유동인구를 노출량으로 사용해 범죄율을 계산한다.

```text
범죄율 = 범죄 발생 건수 / 인구 또는 유동인구 × 10,000
```

현재 생활안전지도 WMS만 사용할 경우 실제 사건 건수나 범죄율이라고 부르지 않고 **범죄위험 등급** 또는 **범죄위험도**라고 부른다. WMS 등급을 격자에 안정적으로 연결할 수 있는 공식 조회값이 있는지 우선 확인하고, 불가능하면 등급 레이어와의 공간 중첩 결과만 사용한다.

## 5. 가설과 검정

### 가설 1: 안전 인프라 접근성과 범죄위험의 관계

- 귀무가설: 시설 충족 개수(0~3) 또는 보호점수와 범죄위험도 사이에 관계가 없다.
- 대립가설: 시설 충족 개수 또는 보호점수가 높을수록 범죄위험도가 낮다.
- 검정: 0·1·2·3개 충족 집단의 분포를 먼저 비교하고, 인구·상권·주거지역·도로 접근성·행정구를 통제한 회귀모형으로 확인한다.

### 가설 2: 세 시설의 결합효과

- 귀무가설: 세 시설 동시 충족의 추가효과가 없다.
- 대립가설: 세 시설을 동시에 충족한 격자는 각 시설의 개별 효과만으로 예상한 것보다 범죄위험도가 더 낮다.
- 검정: `CCTV × 보안등 × Wi-Fi` 상호작용항을 포함한다. 동시에 유사한 인구·토지이용·상권 특성을 가진 삼중 충족 격자와 미충족 격자를 매칭하여 평균 차이와 95% 신뢰구간을 제시한다.

범죄 건수라면 음이항 회귀를 우선 사용하고, WMS의 1~5 등급이라면 순서형 로지스틱 회귀를 사용한다. 공간적으로 가까운 격자의 값이 닮는 현상을 고려해 행정동 단위 군집 표준오차 또는 공간 교차검증을 적용한다.

## 6. 데이터 기반 가중치 산정

초기 가중치와 함께 다음 절차로 학습 가중치를 산정한다.

1. 시설별 점수를 0~1로 표준화한다.
2. 인구·유동인구·토지이용·상권·도로 등 통제변수와 함께 모형에 입력한다.
3. 행정구 또는 공간 블록 단위 교차검증으로 과적합을 확인한다.
4. 시설 하나를 제외했을 때 예측력이 얼마나 떨어지는지 계산한다.
5. 세 시설의 예측력 감소분을 합이 1이 되도록 정규화하여 최종 가중치로 사용한다.
6. 부트스트랩으로 가중치의 95% 신뢰구간을 함께 제시한다.

가중치가 표본에 따라 크게 바뀌면 임의의 단일 숫자를 확정하지 않고 범위로 제시한다.

## 7. 안전도 및 위험 우선순위 점수

범죄위험도 `C`와 보호점수 `P`를 모두 0~100으로 정규화한다. `C`는 높을수록 위험하고 `P`는 높을수록 보호가 강하다.

```text
우선개선점수 R = 0.65 × C + 0.35 × (100 - P)
안전도 S = 100 - R
```

범죄위험을 정책 우선순위의 중심에 두기 위해 0.65를 부여한다. 이 비율도 실제 결과변수로 검증한 뒤 조정한다.

| 안전도 S | 지도 점수 | 의미 |
|---:|---:|---|
| 80 이상 | 5 | 매우 안전 |
| 60 이상 80 미만 | 4 | 안전 |
| 40 이상 60 미만 | 3 | 보통 |
| 20 이상 40 미만 | 2 | 위험 |
| 20 미만 | 1 | 매우 위험 |

고정 구간과 함께 창원시 내 백분위도 표시한다. 고정 구간은 연도별 개선 비교에, 백분위는 현재 시점의 상대적 Top 10 선정에 사용한다.

## 8. Top 10 선정 규칙

1. 우선개선점수 상위 5% 격자를 추출한다.
2. 맞닿은 격자를 하나의 연속 위험구역으로 병합한다.
3. 구역별 평균 위험도, 최고 위험도, 면적, 추정 노출인구를 계산한다.
4. `평균 우선개선점수 × log(1 + 노출인구)`로 정책 우선순위를 정한다.
5. 상위 10개 구역마다 부족한 시설, 권장 설치 위치, 기대 수혜인구를 함께 제시한다.

## 9. 현재 데이터에서 확인된 기준선

- CCTV: 2,810행, 고유 좌표 2,739곳
- 보안등: 27,146곳
- 공공 Wi-Fi: 638행, 고유 좌표 535곳
- 현재 반경 기준에서 Wi-Fi 지점을 기준으로 CCTV 100m와 보안등 50m를 모두 만족하는 지점: 214곳
- 위 214곳을 격자로 묶으면 50m 207개, 100m 198개, 200m 187개 격자

현재 삼각형은 Wi-Fi 위치를 후보점으로 삼기 때문에 도시 전체의 모든 삼중 중첩 면적을 뜻하지 않는다. 최종 분석에서는 각 격자 중심을 기준으로 세 시설 점수를 모두 계산해야 한다.

## 10. 공모전 산출물

- 가설 검정 표: 효과크기, 95% 신뢰구간, p값, 표본 수
- 50m·100m·200m 민감도 비교표
- 안전도 1~5 등급 지도
- 연속 위험구역 Top 10 및 시설 보강 시나리오
- 가중치 변화에 따른 Top 10 순위 안정성 분석
- 데이터 출처, 기준연도, 결측·중복 처리, 한계 명시

