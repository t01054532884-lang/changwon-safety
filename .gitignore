# Local secrets
.env
.streamlit/secrets.toml

# Local geocoding inputs and cache
data/CCTV카메라(방범용)1.xlsx
data/geocoding_cache.json

# Generated and backup files
changwon_map.html
*.bak
__pycache__/
*.py[cod]
.venv/
.codex-tmp/

# Citizen app is maintained in its own Git repository.
citizen/

# 위험신고 SQLite 저장소(런타임 생성 파일 — 실행할 때마다 새로 만들어지므로 커밋하지 않음)
citizen-app/backend/reports.db
